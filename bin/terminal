#! /bin/sh

usb=`ls -1 /dev/ttyUSB*`
nusb=`echo $usb |wc -l`

if [ 1 -ne $nusb ]; then
    echo "** ERROR: cannot find ttyUSB"
    exit 1
fi

echo "# Using $usb"

# exec screen /dev/ttyUSB0 115200
tio --baudrate 115200  $usb
